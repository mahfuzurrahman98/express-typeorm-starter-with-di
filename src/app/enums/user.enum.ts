export enum UserRole {
    GUEST = 'guest',
    ADMIN = 'admin',
    USER = 'user',
}

export enum UserStatus {
    PENDING = 'pending',
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    SUSPENDED = 'suspended',
}
